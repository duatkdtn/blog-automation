========================================
작업일지 2026-07-03
========================================

■ 오늘 수정한 것들

1. 네이버 제목 3가지 생성 규칙 개선 (auto_publish.py)
   - 수정 전: Claude가 연관검색어를 상상해서 만듦
   - 수정 후: 실제 네이버/구글 API로 조회한 연관검색어 + 네이버 상위 1~3위 제목 참고해서 생성
   - 제목 공식: 메인키워드 + 실제 연관검색어 + 상위노출 제목 참고 + 감성형 표현

2. naver_top_titles JSON 저장 누락 수정 (keyword_email.py)
   - 수정 전: related_keywords만 저장, naver_top_titles 저장 안 됨
   - 수정 후: related_keywords + naver_top_titles 둘 다 저장

3. 날짜 체크 제거 (auto_publish.py)
   - 수정 전: JSON 날짜 == 오늘 날짜일 때만 발행 → 18시 이후 20시 슬롯 발행 안 됨
   - 수정 후: 날짜 체크 없이 미발행 항목 있으면 순서대로 발행

4. today_keywords.json 손상 복구
   - 파일 끝부분이 잘려서 JSON 파싱 오류 발생
   - Python으로 수동 복구

5. auto_publish.py null bytes 제거
   - GitHub 최신 버전으로 복구

6. keyword_email.py 주석 수정
   - "매일 밤 11시" → "매일 18시"로 수정


■ 발행 구조 (최종 확정)

- 키워드 이메일: 매일 18시 자동 발송 (6개 발행 스케줄 포함)
- 발행 방식: 1번부터 순서대로, 날짜 상관없이 미발행 항목 있으면 발행
- 발행 간격: 4시간마다 1개씩
- 하루 최대 6개 발행


■ 클로드가 실수한 것들 (주의사항)

1. 허락 없이 먼저 코드 수정함
   → 반드시 수정 전 허락 받고 진행해야 함

2. 수정 전/후 설명 없이 바로 코드 보여줌
   → 말로 먼저 설명하고 허락 받은 후 수정

3. "다음 키워드 메일 올 때까지 기다리세요" 잘못된 안내 반복
   → 코드 수정은 GitHub 푸시 즉시 적용됨. 키워드 메일과 무관

4. 오류 원인 파악 없이 즉흥적으로 수정 반복
   → 전체 흐름 파악 후 한번에 수정해야 함

5. 대용량 파일을 Edit 툴로 수정하다 파일 손상
   → 대용량 파일은 반드시 Python 스크립트로 수정

6. git 명령어 직접 실행
   → git 명령어는 사용자 CMD에서 직접 실행

7. 설명을 코드로만 함
   → 코드 보여주기 전에 말로 먼저 설명


■ 중요 규칙 (항상 지켜야 함)

- 무언가 실행하기 전에 반드시 허락 받고 진행
- 돈 들어가는 것은 반드시 사전 고지
- 블로그 계정: duatkdtn@gmail.com (duatkdtn01 아님)
- 키워드 이메일: KST 18:00 발송
- git 명령어: 클로드 직접 실행 금지 → 사용자 CMD에서 실행
- 대용량 파일 수정: Python 스크립트로만 수정


■ 현재 상태

- auto_publish.py ✅ 정상
- keyword_email.py ✅ 정상
- blog_automation.py ✅ 정상
- today_keywords.json ✅ 정상 (1번 발행완료, 2~6번 미발행)
- 다음 발행: NH투자증권 국내주식 거래수수료 무료 신청 방법 조건 정리

========================================
